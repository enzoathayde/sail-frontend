# Sail - Frontend

## Running locally

1. Config the correct node version with nvm:
```
nvm use 22
```
This command allows get the node v22.22.2, defined in .nvmrc

2. Installling dependencies:
```
npm i
```

3. Run the project with:
```
npx expo start -c 
```
flag -c does a cache clean